<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<assembly xmlns="urn:schemas-microsoft-com:asm.v1" manifestVersion="1.0">
  <dependency>
    <dependentAssembly>
      <assemblyIdentity
        type="win32"
        name="Microsoft.Windows.Common-Controls"
        version="6.0.0.0"
        processorArchitecture="*"
        publicKeyToken="6595b64144ccf1df"
        language="*"
      />
    </dependentAssembly>
  </dependency>
  <!--
    Explicitly request `asInvoker`. Without a requestedExecutionLevel, Windows
    "Installer Detection" auto-elevates any exe whose name contains
    install/setup/update/patch — which our `controller-pack-installer.exe` trips,
    causing `cargo run` to fail with os error 740 (requires elevation). This app
    only writes to the user's controller-pack directory, so standard-user rights
    are correct.
  -->
  <trustInfo xmlns="urn:schemas-microsoft-com:asm.v3">
    <security>
      <requestedPrivileges>
        <requestedExecutionLevel level="asInvoker" uiAccess="false" />
      </requestedPrivileges>
    </security>
  </trustInfo>
</assembly>
